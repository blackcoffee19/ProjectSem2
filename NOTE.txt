composer update

php artisan migrate
php artisan db:seed

NEED require socialite to get GoogleAuth
composer require laravel/socialite
npm install slick-carousel


===//=== Note của hiển ===//===
--- Dash board ---
0. Tuỳ chỉnh lại Route cho dễ nhìn dễ viết
1. Products hoàn thành CRUD - search Name - lọc Category theo <select>
2. Categories hoàn thành CRUD - search Name - lọc Status theo <select>
3. Order hoàn thành CRUD - search Order Name - lọc Status theo <select>
4. Chỉnh lại file header/admin -> avatar, name, email theo database trong phần dropdown

--- User view ---
1. Chỉnh lại file header/user -> phần user dropdown -> nếu là admin thì sẽ hiện link Dashboard, user sẽ không hiện
2. Chỉnh phần hiển thị Featured Categories đúng theo database -> click ra trang chứa sản phẩm đúng loại như vậy
3. Chỉnh phần giá từ .00 không còn nữa cho gọn hơn
4. Chỉnh các link details cho hình ảnh và tên sản phẩm
5. Ẩn phần menu trong file header, ẩn dòng Payment Partners trong footer

--- Database ---
1. thêm $table->string('instruction')->nullable(); trong bảng order
2. thêm $table->boolean('status')->default(true); trong bảng user
3. download và để file sql trong dự án

>> 26/4
1. Làm phần seeders product, Categories, library
2. Thêm hình vào folder product (30 sản phẩm)
3. Sửa nav phần header
4. Fix route
===//=== End note của hiển ===//===

